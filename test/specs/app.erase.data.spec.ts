describe('when requesting to erase all data', () => {
  it.skip('should erase all user data', async () => {
    /*
      Open app
      Download and favorite a meditation
      Assert meditation is favorited and downloaded
      Navigate to Stats
      Add 1 minute for today and yesterday
      Assert cards values at the top and calendar state
      Navigate to Settings -> Home, repeat step c
      Navigate to Home -> Stats, repeat step f
      Navigate to Settings and Clear Data
      Navigate to Home and Stats and check that data is cleared
    */
  })
})
